'use strict';

/**
 * @ngdoc function
 * @name webadminApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the webadminApp
 */

(function () {
  var injectParams = ['$http', '$rootScope', '$scope'];
  var factory = function ($http, $rootScope, $scope) {



  };
  factory.$inject = injectParams;
  angular.module(routeApp).controller('LoginController', factory);
}());
