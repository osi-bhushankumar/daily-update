'use strict';

/**
 * @ngdoc function
 * @name webadminApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the webadminApp
 */

(function () {
  var injectParams = ['$http', '$rootScope', '$scope'];
  var factory = function ($http, $rootScope, $scope) {



  };
  factory.$inject = injectParams;
  angular.module(routeApp).controller('HomeController', factory);
}());

